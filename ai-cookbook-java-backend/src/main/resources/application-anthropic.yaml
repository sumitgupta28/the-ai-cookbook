spring:
  ai:
    model:
      chat: anthropic
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: "claude-sonnet-4-6"
          temperature: 0.7

logging:
  level:
    in.ai.chatbot: DEBUG
